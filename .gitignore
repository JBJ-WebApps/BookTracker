# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files — never commit secrets
.env
.env.*
!.env.example

# Local seeding files (may contain plaintext passwords for initial import only)
seed/
!seed/.gitkeep

# Claude Code local-only settings (permission allowlist, etc.)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
reference/
